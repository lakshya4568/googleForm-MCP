# Environment variables for Google Forms MCP Server

# Google OAuth 2.0 credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# Optional: Path to service account key file (alternative to OAuth)
# GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./path/to/service-account-key.json

# Optional: Default form ID for testing
# DEFAULT_FORM_ID=your_form_id_here

# Optional: Rate limiting configuration
API_RATE_LIMIT_PER_MINUTE=60
API_RATE_LIMIT_PER_HOUR=1000

# Optional: Cache settings
CACHE_TTL_SECONDS=300

# Optional: Server configuration
SERVER_NAME=Google Forms MCP Server
SERVER_VERSION=1.0.0
